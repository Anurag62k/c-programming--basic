#include<stdio.h>
#include<math.h>

int main(){
    int a;
    printf("enter a number");
    scanf("%d",&a);
    float root=sqrt(a);
    printf("the square root of the number is:%f ",root);
    return 0;
}